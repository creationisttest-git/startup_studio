# Nothing is prepended above this line. Checked 2026-09-24 by fetching this file from the live
# site with the cache bypassed: the served file begins here. Bot Preference Sync is on in the
# Cloudflare dashboard and its description says it prepends robots.txt with the configured
# settings; this hostname serves nothing above this text.
#
# WHAT THE ZONE ACTUALLY DOES is set by CATEGORY rather than by crawler name, and it reads
# Search allow, Agent allow, Training disallow. Agent is the category that answers a person's
# question, and it has been allowed all along. So the groups below override nothing. They are an
# explicit statement of intent, kept because this site is meant to be found: it is AGPL, the code
# is public, and the people it is for increasingly ask an assistant rather than a search engine.
#
# One thing here disagrees with the zone and is left visible rather than quietly resolved.
# Cloudflare classifies ClaudeBot, GPTBot, Google-Extended and CCBot as training crawlers, so by
# that classification they meet the one category set to disallow and are refused at the edge
# whatever this file asks for. Only one of the four has been measured: ClaudeBot, 0 allowed of
# 553 requests across the zone in the 30 days to 2026-09-24. Whether to keep asking for the four
# is the site owner's call rather than a tweak, because the zone policy covers every hostname on
# it. The retrieval agents, Claude-SearchBot, OAI-SearchBot and PerplexityBot, are allowed, and
# they are the half that decides whether an assistant can cite this site.

User-agent: ClaudeBot
Allow: /

User-agent: Claude-SearchBot
Allow: /

User-agent: GPTBot
Allow: /

User-agent: OAI-SearchBot
Allow: /

User-agent: Google-Extended
Allow: /

User-agent: PerplexityBot
Allow: /

User-agent: CCBot
Allow: /

User-agent: *
Allow: /

Sitemap: https://startupstudio.projectfreedom.xyz/sitemap.xml
